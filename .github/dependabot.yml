version: 2

updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # Deno has no Dependabot ecosystem; `deno audit` in cd.yaml covers those
  # dependencies. This entry tracks the base image in the root Dockerfile.
  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
